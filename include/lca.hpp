#ifndef LCA_HPP
#define LCA_HPP

#include <string>

// Finds and returns the lowest common ancestor (LCA) of two commits in the commit history
std::string findLowestCommonAncestor(const std::string& commit1, const std::string& commit2);

#endif // LCA_HPP
